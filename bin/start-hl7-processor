#!/usr/bin/ruby

require 'hl7-processor'

config = HL7Processor::Configuration.new(
    port:               5900,
    message_processor:  HL7Processor::Processors::Immediate,
    channels:           [HL7Processor::Channels::Logging]
    )
server = HL7Processor::Host.new(config)

server.start
